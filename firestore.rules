
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // For development only - allows all access
    // WARNING: Change this before going to production!
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
