
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write access during development
    // WARNING: For development only, update with proper rules before production
    match /{document=**} {
      allow read, write;
    }
  }
}
